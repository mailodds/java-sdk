/*
 * MailOdds Email Platform API
 * MailOdds is an email platform for validation, sending, campaigns, deliverability monitoring, and analytics. The API performs multi-layer validation checks, delivers transactional and campaign email with DKIM dual signing, and tracks engagement with privacy-first analytics.  ## Authentication  All API requests require authentication using a Bearer token. Include your API key  in the Authorization header:  ``` Authorization: Bearer YOUR_API_KEY ```  API keys can be created in the MailOdds dashboard.  ## Rate Limits  Rate limits vary by plan: - Free: 10 requests/minute - Starter: 60 requests/minute   - Pro: 300 requests/minute - Business: 1000 requests/minute - Enterprise: Custom limits  ## Response Format  All responses include: - `schema_version`: API schema version (currently \"1.0\") - `request_id`: Unique request identifier for debugging  Error responses include: - `error`: Machine-readable error code - `message`: Human-readable error description  ## Webhooks  MailOdds can send webhook notifications for job completion and email delivery events. Configure webhooks in the dashboard or per-job via the `webhook_url` field.  ### Event Types  | Event | Description | |-------|-------------| | `job.completed` | Validation job finished processing | | `job.failed` | Validation job failed | | `message.queued` | Email queued for delivery | | `message.delivered` | Email delivered to recipient | | `message.bounced` | Email bounced | | `message.deferred` | Email delivery deferred | | `message.failed` | Email delivery failed | | `message.opened` | Recipient opened the email | | `message.clicked` | Recipient clicked a link |  ### Payload Format  ```json {   \"event\": \"job.completed\",   \"job\": { ... },   \"timestamp\": \"2026-01-15T10:30:00Z\" } ```  ### Webhook Signing  If a webhook secret is configured, each request includes an `X-MailOdds-Signature` header containing an HMAC-SHA256 hex digest of the request body.  **Verification pseudocode:** ``` expected = HMAC-SHA256(webhook_secret, request_body) valid = constant_time_compare(request.headers[\"X-MailOdds-Signature\"], hex(expected)) ```  The payload is serialized with compact JSON (no extra whitespace, sorted keys) before signing.  ### Headers  All webhook requests include: - `Content-Type: application/json` - `User-Agent: MailOdds-Webhook/1.0` - `X-MailOdds-Event: {event_type}` - `X-Request-Id: {uuid}` - `X-MailOdds-Signature: {hmac}` (when secret is configured)  ### Retry Policy  Failed deliveries (non-2xx response or timeout) are retried up to 3 times with exponential backoff (10s, 60s, 300s). 
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@mailodds.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.mailodds.models;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.mailodds.models.ProductFacetsCategoriesInner;
import com.mailodds.models.ProductFacetsPriceRangesInner;
import com.mailodds.models.ProductFacetsStoresInner;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.mailodds.JSON;

/**
 * Aggregated facets for the current query filters
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2026-03-17T17:50:09.712858375+01:00[Europe/Amsterdam]", comments = "Generator version: 7.19.0")
public class ProductFacets {
  public static final String SERIALIZED_NAME_CATEGORIES = "categories";
  @SerializedName(SERIALIZED_NAME_CATEGORIES)
  @javax.annotation.Nullable
  private List<ProductFacetsCategoriesInner> categories = new ArrayList<>();

  public static final String SERIALIZED_NAME_PRICE_RANGES = "price_ranges";
  @SerializedName(SERIALIZED_NAME_PRICE_RANGES)
  @javax.annotation.Nullable
  private List<ProductFacetsPriceRangesInner> priceRanges = new ArrayList<>();

  public static final String SERIALIZED_NAME_STORES = "stores";
  @SerializedName(SERIALIZED_NAME_STORES)
  @javax.annotation.Nullable
  private List<ProductFacetsStoresInner> stores = new ArrayList<>();

  public ProductFacets() {
  }

  public ProductFacets categories(@javax.annotation.Nullable List<ProductFacetsCategoriesInner> categories) {
    this.categories = categories;
    return this;
  }

  public ProductFacets addCategoriesItem(ProductFacetsCategoriesInner categoriesItem) {
    if (this.categories == null) {
      this.categories = new ArrayList<>();
    }
    this.categories.add(categoriesItem);
    return this;
  }

  /**
   * Get categories
   * @return categories
   */
  @javax.annotation.Nullable
  public List<ProductFacetsCategoriesInner> getCategories() {
    return categories;
  }

  public void setCategories(@javax.annotation.Nullable List<ProductFacetsCategoriesInner> categories) {
    this.categories = categories;
  }


  public ProductFacets priceRanges(@javax.annotation.Nullable List<ProductFacetsPriceRangesInner> priceRanges) {
    this.priceRanges = priceRanges;
    return this;
  }

  public ProductFacets addPriceRangesItem(ProductFacetsPriceRangesInner priceRangesItem) {
    if (this.priceRanges == null) {
      this.priceRanges = new ArrayList<>();
    }
    this.priceRanges.add(priceRangesItem);
    return this;
  }

  /**
   * Get priceRanges
   * @return priceRanges
   */
  @javax.annotation.Nullable
  public List<ProductFacetsPriceRangesInner> getPriceRanges() {
    return priceRanges;
  }

  public void setPriceRanges(@javax.annotation.Nullable List<ProductFacetsPriceRangesInner> priceRanges) {
    this.priceRanges = priceRanges;
  }


  public ProductFacets stores(@javax.annotation.Nullable List<ProductFacetsStoresInner> stores) {
    this.stores = stores;
    return this;
  }

  public ProductFacets addStoresItem(ProductFacetsStoresInner storesItem) {
    if (this.stores == null) {
      this.stores = new ArrayList<>();
    }
    this.stores.add(storesItem);
    return this;
  }

  /**
   * Get stores
   * @return stores
   */
  @javax.annotation.Nullable
  public List<ProductFacetsStoresInner> getStores() {
    return stores;
  }

  public void setStores(@javax.annotation.Nullable List<ProductFacetsStoresInner> stores) {
    this.stores = stores;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    ProductFacets productFacets = (ProductFacets) o;
    return Objects.equals(this.categories, productFacets.categories) &&
        Objects.equals(this.priceRanges, productFacets.priceRanges) &&
        Objects.equals(this.stores, productFacets.stores);
  }

  @Override
  public int hashCode() {
    return Objects.hash(categories, priceRanges, stores);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class ProductFacets {\n");
    sb.append("    categories: ").append(toIndentedString(categories)).append("\n");
    sb.append("    priceRanges: ").append(toIndentedString(priceRanges)).append("\n");
    sb.append("    stores: ").append(toIndentedString(stores)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("categories", "price_ranges", "stores"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to ProductFacets
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!ProductFacets.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in ProductFacets is not found in the empty JSON string", ProductFacets.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!ProductFacets.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` in the JSON string is not defined in the `ProductFacets` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("categories") != null && !jsonObj.get("categories").isJsonNull()) {
        JsonArray jsonArraycategories = jsonObj.getAsJsonArray("categories");
        if (jsonArraycategories != null) {
          // ensure the json data is an array
          if (!jsonObj.get("categories").isJsonArray()) {
            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `categories` to be an array in the JSON string but got `%s`", jsonObj.get("categories").toString()));
          }

          // validate the optional field `categories` (array)
          for (int i = 0; i < jsonArraycategories.size(); i++) {
            ProductFacetsCategoriesInner.validateJsonElement(jsonArraycategories.get(i));
          };
        }
      }
      if (jsonObj.get("price_ranges") != null && !jsonObj.get("price_ranges").isJsonNull()) {
        JsonArray jsonArraypriceRanges = jsonObj.getAsJsonArray("price_ranges");
        if (jsonArraypriceRanges != null) {
          // ensure the json data is an array
          if (!jsonObj.get("price_ranges").isJsonArray()) {
            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `price_ranges` to be an array in the JSON string but got `%s`", jsonObj.get("price_ranges").toString()));
          }

          // validate the optional field `price_ranges` (array)
          for (int i = 0; i < jsonArraypriceRanges.size(); i++) {
            ProductFacetsPriceRangesInner.validateJsonElement(jsonArraypriceRanges.get(i));
          };
        }
      }
      if (jsonObj.get("stores") != null && !jsonObj.get("stores").isJsonNull()) {
        JsonArray jsonArraystores = jsonObj.getAsJsonArray("stores");
        if (jsonArraystores != null) {
          // ensure the json data is an array
          if (!jsonObj.get("stores").isJsonArray()) {
            throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `stores` to be an array in the JSON string but got `%s`", jsonObj.get("stores").toString()));
          }

          // validate the optional field `stores` (array)
          for (int i = 0; i < jsonArraystores.size(); i++) {
            ProductFacetsStoresInner.validateJsonElement(jsonArraystores.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!ProductFacets.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'ProductFacets' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<ProductFacets> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(ProductFacets.class));

       return (TypeAdapter<T>) new TypeAdapter<ProductFacets>() {
           @Override
           public void write(JsonWriter out, ProductFacets value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public ProductFacets read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of ProductFacets given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of ProductFacets
   * @throws IOException if the JSON string is invalid with respect to ProductFacets
   */
  public static ProductFacets fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, ProductFacets.class);
  }

  /**
   * Convert an instance of ProductFacets to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

